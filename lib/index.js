import postcss from 'postcss'

export default postcss.plugin('postcss-bgc-rgba-fallback', (opts = {}) => {
  // Work with options here
  return (root, result) => {

    // Transform CSS AST here

  }
})
